## What changed

## Evidence

- [ ] Tests added or updated
- [ ] `ruff`, strict `mypy`, and `pytest` pass
- [ ] No API key, `.env`, raw cache, full minutes, or generated index is committed
- [ ] Parser changes include reviewed provenance-preserving fixtures
- [ ] Retrieval changes include measured qrels/benchmark results
