name: Parser fixture
description: Propose an official minutes format that needs parser coverage
title: "[Fixture]: "
labels: [parser, fixture, "good first issue"]
body:
  - type: input
    id: source
    attributes:
      label: Official record.assembly.go.kr URL
    validations:
      required: true
  - type: textarea
    id: format
    attributes:
      label: Format difference
      description: Describe speaker markers or layout without pasting the full document.
    validations:
      required: true
