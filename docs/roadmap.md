# Roadmap

## Before the next public release

- Validate fresh-cache live research across unrelated committees, dates, and bill states.
- Add a setup smoke test for Claude Code, Codex, Gemini CLI, and Claude Desktop.
- Report which meetings were inspected and warn when live evidence is sparse.
- Add retry and upstream quota diagnostics without leaking credentials.
- Publish only after all README commands work from the packaged artifact.

## Later

- Optional semantic index over the user's accumulated cache.
- Additional official Assembly datasets where their contracts are verified.
- Authenticated bring-your-own-key remote transport for web-only MCP clients.
